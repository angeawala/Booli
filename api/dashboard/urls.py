# dashboard/urls.py
"""from django.urls import path
from .views import (
    ClientDashboardView, SellerDashboardView, VisitorDashboardView, AdminDashboardView
)

app_name = 'dashboard'

urlpatterns = [
    path('client/', ClientDashboardView.as_view(), name='client_dashboard'),
    path('seller/', SellerDashboardView.as_view(), name='seller_dashboard'),
    path('visitor/', VisitorDashboardView.as_view(), name='visitor_dashboard'),
    path('admin/', AdminDashboardView.as_view(), name='admin_dashboard'),
]"""