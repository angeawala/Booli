# customers/urls.py
"""from django.urls import path
from .views import (
    ListCustomersView, GetCustomerView, UpdateCustomerView, DeleteCustomerView, ContactCustomerView
)

app_name = 'customers'

urlpatterns = [
    path('', ListCustomersView.as_view(), name='list_customers'),
    path('<str:id>/', GetCustomerView.as_view(), name='get_customer'),
    path('<str:id>/update/', UpdateCustomerView.as_view(), name='update_customer'),
    path('<str:id>/delete/', DeleteCustomerView.as_view(), name='delete_customer'),
    path('contact/', ContactCustomerView.as_view(), name='contact_customer'),
]"""